import styled from 'styled-components';
import { Container } from '@mui/material';

export const PageLayoutContainer = styled('div')`
    position: relative;
    z-index: 1;
    cursor: pointer;
`

export const PageLayoutMenu = styled(Container)`
    position: absolute;
    top: 2px;
    left: -24px;
`

